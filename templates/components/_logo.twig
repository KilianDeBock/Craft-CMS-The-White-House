<a href="{{ currentSite.baseUrl }}">
    {# Get image #}
    {% set image = branding.image.one() %}

    {# Check if image is svg, use svg if, else img. #}
    {% if image and image.extension == 'svg' %}
        {{ svg(image) }}
    {% else %}
        <img class="logo" src="{{ image.url }}" alt="{{ image.title }}"/>
    {% endif %}
</a>